# RENDER DEPLOYMENT - MINIMAL CONFIG

# Database - Railway MySQL (keep using it)
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA - Minimal settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.sql.init.mode=never

# Server - with context path for /hub
server.port=${PORT:8080}
server.servlet.context-path=/hub

# PayProp Configuration - Disabled for initial deployment
payprop.enabled=false

# Dummy PayProp configuration (safe defaults when disabled)
payprop.api.base-url=https://uk.payprop.com/api/agency/v1.1
payprop.api.key=dummy_api_key_12345
payprop.webhook.enabled=false
payprop.webhook.url=https://spoutproperty.com/hub/api/webhooks/payprop
payprop.webhook.secret=dummy_secret_key_12345
payprop.webhook.verify-signature=false

# PayProp Sync Settings - All disabled
payprop.sync.pull-tags-on-startup=false
payprop.sync.auto-create-portfolios=false
payprop.sync.conflict-resolution=MANUAL

# Portfolio Configuration - Disabled
portfolio.auto-sync.enabled=false

# Exclude PayProp beans from loading
spring.main.allow-bean-definition-overriding=true

# Minimal logging
logging.level.root=WARN

# Required environment variables with safe defaults
weather.api.key=${WEATHER_API_KEY:dummy-key}
company.name=${COMPANY_NAME:Test Company}
app.base-url=https://spoutproperty.com/hub

# Disable OAuth temporarily (we'll add back later)
# spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
# spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

# Database connection limits
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.connection-timeout=10000
